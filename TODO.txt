-*Make sure the markers in recent earthquake is giving coords to the subscription location when selected by the user (onClick)
[--]subscriptionLocation component that keeps track of the location that user wants, only holds lat and long
--change the create alert button to work with the side panel (it should only be visible during currentView is recent)